<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.TayabaStrack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorControlActivated">@color/blue</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <!-- You can also override other colors if needed -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="CustomAlertDialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="android:background">#004aad</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="Theme.TayabaStrack" parent="Base.Theme.TayabaStrack" />
    <style name="CustomDropdownStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="android:popupBackground">#004aad</item>
    </style>

    <!-- Add this new splash screen style -->
    <style name="SplashTheme" parent="Theme.TayabaStrack">
        <item name="android:windowBackground">@color/blue_primary</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
    </style>
</resources>
